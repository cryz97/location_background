name: test_location_background
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  location:
    git:
      url: git://github.com/Lyokone/flutterlocation.git
      ref: background_location

  cupertino_icons: ^0.1.2
  http: ^0.12.0+1


dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:

  uses-material-design: true